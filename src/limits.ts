export const LIMITS = Object.freeze({
  tasks: 8,
  concurrency: 5,
  titleChars: 120,
  inputBytes: 256 * 1024,
  timeoutMs: 10 * 60 * 1000,
  graceMs: 1500,
  updateMs: 250,
  animationMs: 80,
  answerBytes: 8 * 1024,
  recordBytes: 16 * 1024 * 1024,
  diagnosticChars: 400,
  activityChars: 160,
});
